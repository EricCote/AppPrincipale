@model IEnumerable<AppPrincipale.Models.Category>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

@foreach (var item in Model)
{
    <h1>
        @Html.ActionLink(item.Name,"Edit",new { id=item.CategoryID })
    </h1>
        
            @Html.DisplayFor(modelItem => item.Products)
        
}

<div class="col-lg-4 col-md-6 ">
    @Html.Partial("_Commentaires",new Commentaire())
    </div>


</div>


